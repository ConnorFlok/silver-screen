// Actor data is now fetched live from TMDB via:
//   searchPeople(query)       → /search/person
//   getPersonMovieCredits(id) → /person/{id}/movie_credits
//
// See src/utils/tmdb.js for these functions.
// The static ACTORS index has been removed.
